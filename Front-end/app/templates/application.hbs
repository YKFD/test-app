<AppHeader/>
{{#if (compare target.currentPath "index")}}
  {{page-title "Test-app"}}
  <div style="margin-top: 120px;">
  {{#each posts as |post|}}
    <h3 class="type-title">{{post.title}}</h3>
    <PostButton @url={{post.url}} @data={{post.data}} />
  {{/each}}
  </div>
{{/if}}
{{outlet}}