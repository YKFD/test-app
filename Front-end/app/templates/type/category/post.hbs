{{page-title @model.title}}
<div class="post-block">
  <h1 class="post-title">{{@model.title}}</h1>
  <p class="post-info">{{@model.body}}</p>
  <p class="post-category">{{@model.category}}</p>
</div>
{{outlet}}